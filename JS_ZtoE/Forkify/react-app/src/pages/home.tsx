const HomePage = () => {
  return null;
};

export default HomePage;

export const loader = () => {
  // window.sessionStorage.setItem("token", "abcd");
  console.log("'root' route loader start");

  return null;
};
